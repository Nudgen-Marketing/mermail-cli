interface:
  display_name: "Mermail Cli"
  short_description: "Repo-specific patterns and workflows for mermail-cli"
  default_prompt: "Use the mermail-cli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true